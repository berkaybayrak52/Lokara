{
  "name": "@lokara/heating-engine",
  "version": "0.0.1",
  "private": true,
  "description": "Pure heating + CO₂ cost engine (HKVO, CO2KostAufG). Built at M2. Depends only on @lokara/domain.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@lokara/domain": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
