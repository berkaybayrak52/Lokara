[project]
name = "lokara-export-engine"
version = "0.1.0"
description = "Pure Anlage-V and DATEV export engine"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/export_engine"]
