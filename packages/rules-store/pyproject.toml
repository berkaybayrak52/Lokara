[project]
name = "lokara-rules-store"
version = "0.1.0"
description = "Versioned legal rules with as-of law dates (skeleton; ported in Phase C)"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/lokara_rules_store"]

