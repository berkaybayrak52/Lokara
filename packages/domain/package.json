{
  "name": "@lokara/domain",
  "version": "0.0.1",
  "private": true,
  "description": "Normalized value objects shared by engines and adapters. Pure: no framework, no DB, no vendor SDK.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "decimal.js": "^10.5.0"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
