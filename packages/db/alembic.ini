# Alembic — migrations for the Lokara schema (run as the owner role, DIRECT_URL).
# The connection URL is resolved in alembic/env.py from DbSettings — never here.
[alembic]
script_location = %(here)s/alembic
prepend_sys_path = %(here)s/src
