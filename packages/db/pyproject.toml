[project]
name = "lokara-db"
version = "0.1.0"
description = "SQLAlchemy 2.0 models + Alembic migrations + RLS (skeleton; built in Phase B)"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/lokara_db"]

