[project]
name = "lokara-pdf"
version = "0.1.0"
description = "Playwright-for-Python HTML-to-PDF service (skeleton; built in Phase D)"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/lokara_pdf"]

