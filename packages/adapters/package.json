{
  "name": "@lokara/adapters",
  "version": "0.0.1",
  "private": true,
  "description": "Port interfaces + stub implementations for every paid/external edge. No vendor SDK leaves this package.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@lokara/domain": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
