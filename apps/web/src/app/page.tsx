import { PortalEntry } from '@/features/portal/portal-entry';

/** Entry point: resolve the caller's relationships → enter the portal.
 * The Phase F token/stack showcase lives on at /styleguide. */
export default function Home() {
  return <PortalEntry />;
}
