[project]
name = "lokara-api"
version = "0.1.0"
description = "Lokara FastAPI backend (skeleton; FastAPI lands in Phase E)"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/lokara_api"]

